# RSC — PROD stack for VPS self-host. Caddy terminates HTTPS and fronts
# everything; core publishes NO host ports (backend-only isolation). Federation
# (WebSub/rssCloud) is ON. Run: docker compose -f compose.prod.yaml up -d --build
services:
  caddy:
    image: caddy:2-alpine
    container_name: rsc-caddy
    depends_on:
      web:
        condition: service_healthy
      core:
        condition: service_healthy
    environment:
      RSC_DOMAIN: ${RSC_DOMAIN:?Set RSC_DOMAIN in .env}
      MAILPIT_USER: ${MAILPIT_USER:-mail}
      MAILPIT_PASSWORD_HASH: ${MAILPIT_PASSWORD_HASH:?Set MAILPIT_PASSWORD_HASH (run scripts/generate-env.sh)}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    restart: unless-stopped
    networks: [frontend, backend] # F-2: needs backend to reach core:8787

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
    container_name: rsc-web
    environment:
      CORE_API_URL: http://core:8787
      PORT: "3000"
      ORIGIN: https://${RSC_DOMAIN:?Set RSC_DOMAIN in .env}
      ADDRESS_HEADER: X-Forwarded-For # F-3: getClientAddress() behind Caddy
      XFF_DEPTH: "1"
    depends_on:
      core:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/', r => process.exit(r.statusCode < 500 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 15s
    deploy:
      resources:
        limits:
          memory: 512M
    restart: unless-stopped
    networks: [frontend, backend]

  core:
    build:
      context: .
      dockerfile: docker/Dockerfile.core
    container_name: rsc-core
    environment:
      RSC_AUTH_SECRET: ${RSC_AUTH_SECRET:?Set RSC_AUTH_SECRET (run scripts/generate-env.sh)}
      RSC_TOKEN: ${RSC_TOKEN:?Set RSC_TOKEN (run scripts/generate-env.sh)}
      RSC_DB: /data/textcaster.db
      RSC_PUBLIC_URL: https://${RSC_DOMAIN:?Set RSC_DOMAIN in .env}
      RSC_WEB_ORIGIN: https://${RSC_DOMAIN}
      RSC_WEBSUB: ${RSC_WEBSUB:-self}
      RSC_RSSCLOUD: ${RSC_RSSCLOUD:-on}
      RSC_PUSH_IN: ${RSC_PUSH_IN:-on}
      RSC_SMTP_URL: ${RSC_SMTP_URL:-smtp://mailpit:1025}
      RSC_MAIL_FROM: ${RSC_MAIL_FROM:-rsc@${RSC_DOMAIN}}
    volumes:
      - core-data:/data
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8787/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    deploy:
      resources:
        limits:
          memory: 512M
    restart: unless-stopped
    networks: [backend] # NO published ports — reachable only via Caddy/web

  mailpit:
    image: axllent/mailpit:latest
    container_name: rsc-mailpit
    environment:
      MP_WEBROOT: /mail
    restart: unless-stopped
    networks: [frontend, backend]

volumes:
  core-data:
  caddy-data:
  caddy-config:

networks:
  frontend:
  backend:
